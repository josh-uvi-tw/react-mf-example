import React from "react"
export default function Header() {
    return (
        <div style={{backgroundColor: "lightblue", fontSize: "bold", color: "#fff", justifyItems: "center", alignItems: "center"}}>
            Micro Frontend Header from remote app
        </div>
    )
}